# Activates the Spark reporter (the standard beautiful HTML theme)
extent.reporter.spark.start=true

# Sets the output folder and file name
extent.reporter.spark.out=test-output/SparkReport/Spark.html